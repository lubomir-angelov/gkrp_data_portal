# Over-engineering Review — Work Items

Source: repo-wide ponytail-audit (over-engineering only; correctness/security routed separately).
Mark `Status` = `done` when a row is complete. After each row run:

```bash
ruff check . && ruff format --check . && python -m pytest
```

Risk = chance of breaking existing functionality:
- **high** — can break live behavior; must verify in a real browser session after the change
- **medium** — behavior-adjacent or workflow-affecting; verify carefully (tests + targeted manual check)
- **low** — dead code, verified zero app callers by grep

Paths are relative to `gkrp_data_portal/`.

| # | Status | Risk | Work item | Main files |
|---|--------|------|-----------|------------|
| 1 | pending | medium | Delete dead Python-side histogram fallback: raw-rows branch of `build_histogram`/`build_histogram_series`, the `rows` param, and `norm_bucket` (all 6 call sites always pass `pre_aggregated` from SQL). Rewrite `test_analytics_common.py` histogram tests to cover the surviving `pre_aggregated` path (currently untested — the load-bearing part of this row). | `src/gkrp_data_portal/ui/pages/analytics_common.py`, `ui/pages/analytics_chart*.py` (3 call-site signatures), `tests/test_analytics_common.py` |
| 2 | pending | medium | Deduplicate WHERE-clause construction in analytics_repo: extract one `_add_ilike_filters(clauses, params, prefix, label_to_col, filters)` helper replacing the 7 copy-pasted ILIKE blocks and the 3 label→column maps duplicated between `_apply_*_filters` and `build_chart_histogram`'s inline maps. The copies are NOT identical (param prefixes, `o.primary_` vs subquery `o_primary`) — add tests for `build_chart_histogram` filter maps before deleting the copies. | `src/gkrp_data_portal/ui/repository/analytics_repo.py`, `tests/test_analytics_repo.py` |
| 3 | pending | low | Delete q1-era leftovers with zero app callers: `query_q1_layers_fragments`, the `"q1"` free-text branch in `_build_where`, `extract_image_urls`, `_extract_layer_filters`, `CHART_MAX_FETCH`, and their tests. | `src/gkrp_data_portal/ui/repository/analytics_repo.py`, `ui/pages/analytics_common.py`, `tests/test_analytics_repo.py`, `tests/test_analytics_common.py` |
| 4 | pending | low | Delete dead controls on both chart pages: `sel_limit` + its limit calc in `_read_filters` (limit never passed to `build_chart_histogram`), `use_all_rows` toggle (never read), stale "25 000 rows" / "max" info labels, and the 4 orphaned locale keys (`chart_fetch_info*`, `limit_max_info*` stays for table page — remove only unused ones). | `src/gkrp_data_portal/ui/pages/analytics_chart_fragments.py`, `analytics_chart_finds.py`, `ui/pages/analytics_common.py` (LOCALE) |
| 5 | pending | high | Shrink `_set_chart` in both chart pages from the NiceGUI-version compat ladder (identical try/except branches, `props()` fallback, RuntimeError) to `chart.figure = figure; chart.update()` plus the existing resize JS. Remove the `chart_type_debug` labels. The ladder likely exists because the obvious path failed on nicegui 3.5.0 — verify in browser: filter change re-renders chart, PNG/JPG download and PDF print work. | `src/gkrp_data_portal/ui/pages/analytics_chart_fragments.py`, `analytics_chart_finds.py` |
| 6 | pending | low | Delete `get_session()` (FastAPI-style dependency, app is NiceGUI-only) and `require_user` (only `require_admin` is used by admin.py), plus their tests. | `src/gkrp_data_portal/db/session.py`, `auth/deps.py`, `tests/test_auth_deps.py`, `tests/test_session.py` |
| 7 | pending | medium | Drop the `markdown` dependency: NiceGUI `ui.markdown()` renders markdown natively (GFM tables/fenced code). Change the 3 `_load_*_guide` helpers to return `read_text()` and remove the dep from pyproject. Current code feeds pre-rendered HTML into `ui.markdown` — eyeball CHART.md / ANALYTICS_TABLE_HELP.md rendering (tables, code blocks, inline HTML) in the help dialogs after the switch. | `src/gkrp_data_portal/ui/pages/analytics_chart.py`, `analytics_chart_fragments.py`, `analytics_chart_finds.py`, `pyproject.toml` |
| 8 | pending | medium | Clean up declared deps: remove `fastapi==0.128.0` (nothing imports it; NiceGUI ships it), fix the broken pin `"loguru==0.7.3,"` → `"loguru==0.7.3"`, and remove `load_dotenv` from `main.py` (Makefile `run` already sources `.env`; python-dotenv is not a declared dep). Dropping dotenv changes behavior for anyone who runs `python -m gkrp_data_portal.main` directly without `make run` (`.env` no longer loaded). | `pyproject.toml`, `src/gkrp_data_portal/main.py` |
| 9 | pending | low | Remove the duplicate logging stack: delete `core/logging.py` and its `configure_logging()` call; switch the stdlib `logging` in `db/session.py` to loguru (already a nicegui dep and used in email.py / analytics_chart.py). Update `tests/test_logging.py` accordingly. Log output style changes only; uvicorn configures its own handlers. | `src/gkrp_data_portal/core/logging.py`, `main.py`, `db/session.py`, `tests/test_logging.py` |
| 10 | pending | medium | Delete dead files: `requirements.txt` (duplicates pyproject, referenced nowhere), `opencode.json.before-cce` (config backup), and `restore_backup_fix_20_05.sh` (one-off, unreferenced by any Makefile/script — confirm with the team it is no longer the manual restore path before deleting). | repo root, `gkrp_data_portal/requirements.txt` |
| 11 | pending | medium | Prune dead entries and small duplicates: ~17 `_SORT_DIR` entries naming hidden columns, dead frozenset members (`f_recrodenteredby`, `f_img_url`, `f_han`, `f_imageurl`, `p_ornamentid`, `o_ornament`, `encrustcolor`, `fi_findid_`), second copy of `_in_list` (keep in one models module), `SESSION_USER_KEY` redefined in dev_login.py (import from auth.deps). Cross-check every "dead" entry against actual model columns first — one wrong deletion re-exposes a hidden column in the table UI. Note: `f_note`/`f_inventory` in `_GROUPBY_EXCLUDE` are live intentional excludes, not dead. | `src/gkrp_data_portal/ui/pages/analytics_table.py`, `ui/pages/analytics_common.py`, `analytics_chart_fragments.py`, `models/archaeology.py`, `models/auth.py`, `ui/pages/dev_login.py` |
| 12 | pending | low | Remove debug leftovers: the two `logger.info("DEBUG first_row_*", ...)` lines in the CSV endpoint (log full first row on every export) and the unused `/api/analytics/health` endpoint (no consumer in compose/Makefile/scripts — confirm no external uptime monitor hits it before removing). | `src/gkrp_data_portal/ui/pages/analytics_chart.py` |
| 13 | pending | medium | Security cleanup (not over-engineering, flagged by audit): `setup.sh` in the working tree holds a live `STORAGE_SECRET` + DB password in plaintext. Delete it (values live in `.env`) and rotate `STORAGE_SECRET` if it was ever shared or committed — rotation invalidates all NiceGUI storage (users logged out, general storage wiped). Confirm `.env`/`setup.sh` stay git-ignored. | `setup.sh`, `.gitignore` |
